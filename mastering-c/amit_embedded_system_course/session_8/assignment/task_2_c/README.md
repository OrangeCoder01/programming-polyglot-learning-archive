# Task 2:
Question:
````
    (2) Write a C function to return the index of LAST occurrence of
        a number in a given array. 
        If the item is not in the list return -1

        Example:
        Array = {1,2,3,4,4,4}
        The required number is 4 it should return 5
````
Explanation:
````
    This program uses function with array parameter whereas processing it to find the index of the last element that is 
    equivalent to the target number. 
````
```c 
        int last_occurence_with_index(int arr[], int size, int target) { return index; } 
```
<br>

|Input:                         |Output:                                                |Rules:                |
|-------------------------------|-------------------------------------------------------|----------------------|
| (1) Array size (integer).     |(1) The printing of the index of the element with last | (1) Size must be between (1~100).|
| (2) Element's value (integer).|    occurence + input array or printing "Not found"    | (2) Elements value must be between (-60,000 ~ 60,000).|
| (3) Target value (integer).   |   in case if a the element is not found.          |

<br>
<br>

Why building this program:
````
    For practise using the functions with array parameter.
````