# Task 3:
Question:
```
    (3) Write a C function to swap the contents of two arrays with
        the same length.
```
Explanation:
````
    Swapping elements of two arrays with equal size, inputed by the user, by using function:
````
```c
        void array_swap(int array_1[], int array_2[], int size) {} /* Returns by reference the change happend in both arrays*/
```
<br>


|Input:                         |Output:                                                |Rules:                |
|-------------------------------|-------------------------------------------------------|----------------------|
| (1) Array size (integer).     |(1) The printing of the original arrays before       | (1) Size must be between (1~100).|
| (2) Element's value (integer).|    swapping and after swapping (text + integers)    | (2) Elements value must be between (-60,000 ~ 60,000).|

<br>
<br>

Why building this program:
```
    Practise using function with multiple array parameters. 
```