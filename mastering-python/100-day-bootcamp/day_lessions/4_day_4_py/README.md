# Day 4:
Learned:
```
    (1) Importing Modules.

    (2) "random" Module:
        (2.1) randint (random.randint())

        (2.2) random (random.random()).
        
        (2.3) uniform (random.uniform())

        (2.4) choice (random.choice())

    (3) Arrays and array's indices:
```